services:
  pingsnest-launch:
    build:
      context: ./html_folder/launch
      dockerfile: Dockerfile
    container_name: pingsnest-launch-countdown
    ports:
      - "8085:80"
    restart: unless-stopped
